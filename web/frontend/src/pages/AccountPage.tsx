import { FormEvent, useState } from 'react'
import { useAuth } from '../context/AuthContext'
export function AccountPage() { const { user, privacy, updatePrivacy } = useAuth(); const [notice, setNotice] = useState(''); if (!user || !privacy) return null; async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); try { await updatePrivacy({ retention_mode: data.get('retention'), allow_external_services: data.get('external') === 'on' }); setNotice('Preferências atualizadas.') } catch (error) { setNotice(error instanceof Error ? error.message : 'Falha ao atualizar.') } } return <div className="app-page"><div className="page-heading"><div><p className="eyebrow">CONTA</p><h1>{user.name}</h1><p>{user.email}</p></div></div><section className="app-panel"><h2>Privacidade</h2><p>Os arquivos não são mantidos após processamento, salvo opção futura e expressa de retenção. Resultados só integram o histórico conforme sua escolha.</p><form onSubmit={submit} className="privacy-form"><label>Retenção padrão<select name="retention" defaultValue={privacy.retention_mode}><option value="PRIVATE">Privada — sem histórico permanente</option><option value="RESULT_ONLY">Manter somente o resultado técnico</option><option value="FILE_AND_RESULT" disabled>Arquivo e resultado — futura</option></select></label><label className="check-line"><input type="checkbox" name="external" defaultChecked={privacy.allow_external_services} /> Permitir integrações externas quando explicitamente informadas</label><button className="button" type="submit">Salvar preferências</button></form>{notice && <p role="status">{notice}</p>}<dl className="privacy-facts"><div><dt>Arquivos armazenados</dt><dd>Nenhum armazenamento permanente disponível.</dd></div><div><dt>Consentimentos</dt><dd>Termos e Política de Privacidade versionados.</dd></div><div><dt>Exportar dados</dt><dd>Planejado.</dd></div><div><dt>Excluir dados</dt><dd>Solicitação administrativa — fluxo automatizado pendente.</dd></div></dl></section></div> }

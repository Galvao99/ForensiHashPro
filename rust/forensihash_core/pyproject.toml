[build-system]
requires = ["maturin>=1.9,<2.0"]
build-backend = "maturin"

[project]
name = "forensihash-core"
requires-python = ">=3.12"
dynamic = ["version"]

[tool.maturin]
module-name = "forensihash_core"
features = ["pyo3/extension-module"]